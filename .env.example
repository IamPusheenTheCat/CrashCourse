# 开发：可复制为 `.env.development.local` 覆盖行为（可选）。
# 生产构建：复制 `.env.production.example` → `.env.production` 并填入真实 API；
# `.env.production` 已在 .gitignore 中，不要提交。

# 自定义 API 根（须含 /api/v1，无末尾斜杠）。不设时：
# - vite dev：同源 /api/v1 → 代理到本机 8000
# - vite build：须本地存在 `.env.production`，或在 CI 里注入 VITE_API_BASE_URL
# VITE_API_BASE_URL=https://your-api.example.com/api/v1
